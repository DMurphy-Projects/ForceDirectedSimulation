package Model.UpdateStrategy;

import Model.ForceNode;

public class UpdateStrategyFixed implements IUpdateStrategy {
    public void update(ForceNode node) {
        //no need to update if they are fixed
    }
}
